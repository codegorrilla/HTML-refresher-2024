export default function bio(name, age, job){
    // const name = 'Ram'
    // const age = 40
    // const job = 'designer'
    // return name + ' is ' + age + ' years old and working as ' + job

    return `${name} is of ${age} years old and working as ${job}`
}